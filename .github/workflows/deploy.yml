name: "Trigger Cloudflare Build"

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Cloudflare Pages Build
        run: |
          curl -X POST "${{ secrets.CLOUDFLARE_WEBHOOK_URL }}"
